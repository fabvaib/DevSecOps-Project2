

apiVersion: v1
kind: Service

metadata:
  name: dso-project2-service

spec:
  type: NodePort

  selector:
    app: dso-project2

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
